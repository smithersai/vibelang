package main

import (
	"fmt"
	"os"

	"github.com/microsoft/TypeScript/tsc/internal/smithers"
)

func main() {
	if len(os.Args) > 2 || (len(os.Args) == 2 && os.Args[1] != "--version") {
		fmt.Fprintln(os.Stderr, "usage: smithersc [--version]")
		os.Exit(2)
	}
	fmt.Print(smithers.Marker())
}
