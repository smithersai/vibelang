# Diagnostics [Every VIBE code the corpus observes, with the message the compiler emits]

{/* Generated by scripts/diagnostics-reference.mjs from the conformance corpus. Do not edit by hand; run the script. */}

This page is generated from the conformance corpus by running every case through the native-backed SDK profile (the runner's historical `js` label) and recording the diagnostics it emits. It lists **71** observed codes and **56** implementation-defined codes this SDK run does not observe. Regenerate it with `node scripts/diagnostics-reference.mjs`.

A diagnostic code names a rule. The message names the construct that broke it and, where the rule has one, the remedy. The corpus case is an example program that draws the code, and the citation is the specification sentence the case pins.

Diagnostics beginning `TS` are TypeScript's own and are not listed here.

## Language, Results, and foreign boundaries

| Code | Message the compiler emits | Corpus case | Specification |
| --- | --- | --- | --- |
| `VIBE1000` | `source does not match the supported .vibe grammar: Unexpected keyword or identifier.`<br/>`source does not match the supported .vibe grammar: Expression expected.` | `19-retired-syntax/runtime-type-modifier-is-not-claimed` (+1) | [Types as Values](/specification/comptime), [Throw Statements](/specification/control-flow) |
| `VIBE1001` | `the '!T' return marker was removed; use Result&lt;T, E&gt;`<br/>`expression-position if grammar was withdrawn; use existing TypeScript expressions`<br/>… and 15 more wording(s) | `19-retired-syntax/bang-return-marker-is-retired` (+20) | [Inference](/specification/failures), [specification/control-flow](/specification/control-flow) |
| `VIBE1101` | `explicit return type cannot represent recoverable failures {Missing}; use Result&lt;A, E&gt;`<br/>`explicit return type cannot represent recoverable failures {Panic}; use Result&lt;A, E&gt;` | `02-unwrap-propagation/a-fallible-generator-is-rejected` (+30) | [Refusal Conditions](/specification/failures), [Foreign Boundary](/specification/compatibility) |
| `VIBE1102` | `exported fallible functions must spell Result&lt;A, E&gt; (or Promise&lt;Result&lt;A, E&gt;&gt;) in their public contract` | `01-result-lifting/exported-fallible-needs-result-contract` | — |
| `VIBE1103` | `recoverable throw values must extend Error; use panic(...) for an unknown defect` | `01-result-lifting/a-thrown-const-tuple-is-not-a-recoverable-error` (+2) | [specification/failures](/specification/failures) |
| `VIBE1104` | `Result contract omits reachable failures {Boom}` | `01-result-lifting/contract-omits-reachable-failure` (+2) | [specification/failures](/specification/failures) |
| `VIBE1106` | `an authored generator cannot carry a non-empty failure row`<br/>`an authored generator cannot carry a non-empty capability requirement row` | `02-unwrap-propagation/a-fallible-generator-is-rejected` (+1) | [Refusal Conditions](/specification/failures), [Inference](/specification/requirements) |
| `VIBE1107` | `class 'static {}' initialization blocks execute outside every checked function channel and are not analyzed or lowered; use static field initializers or an explicit checked function` | `09-foreign-calls/a-panic-in-a-static-initializer-block-is-still-refused` (+1) | [Panic Does Not Widen a Return Type](/specification/failures), [Function Type](/specification/type-system) |
| `VIBE1150` | `duplicate Error class name 'Missing' cannot receive a stable module-local identity`<br/>`duplicate Error class name 'Failed' cannot receive a stable module-local identity` | `04-nominal-errors/duplicate-error-class-name-is-rejected` (+1) | [Error Classes](/specification/failures), [Durable Boundary Contract requires an unambiguous durable failure identity](/specification/durable-execution) |
| `VIBE1201` | `Result.ok is a compiler hook, not an author-facing constructor; use ordinary return/throw lifting`<br/>`Result.err is a compiler hook, not an author-facing constructor; use ordinary return/throw lifting` | `01-result-lifting/a-computed-result-ok-is-a-compiler-hook` (+1) | [Result Composition](/specification/failures) |
| `VIBE1202` | `postfix ! propagation requires an enclosing Result-returning function` | `02-unwrap-propagation/unwrap-at-top-level-is-rejected` | — |
| `VIBE1203` | `nested Result normalization is not specified; make the conversion explicit` | `19-retired-syntax/result-nesting-remains-rejected` | [Result Composition](/specification/type-system) |
| `VIBE1205` | `postfix ! propagation inside a JavaScript try statement with a catch clause is not lowered because the failure exit unwinds the computation past this catch clause: finally blocks and using disposals still run, catch never observes the failure, and delivering it as a thrown exception is prohibited; move the propagation point outside the try or consume the value explicitly` | `02-unwrap-propagation/unwrap-inside-try-with-catch-is-rejected` | — |
| `VIBE1206` | `Result.unwrap() is no longer the propagation spelling; use postfix !` | `19-retired-syntax/a-computed-unwrap-is-the-retired-spelling` (+1) | — |
| `VIBE1207` | `postfix ! requires a Result operand; TypeScript non-null assertions are unavailable in .vibe` | `02-unwrap-propagation/an-unawaited-promise-bang-forwarded-into-a-return-reports-only-the-operand` (+5) | [Accepted Placements](/specification/failures), [specification/type-system](/specification/type-system) |
| `VIBE1251` | `Error.match requires an object literal so nominal cases can be checked and lowered` | `04-nominal-errors/error-match-requires-an-object-literal` | — |
| `VIBE1252` | `Error match cases must use static Error class names and function handlers` | `04-nominal-errors/error-match-cases-must-be-static-class-names` | [Error Prototype](/specification/failures) |
| `VIBE1253` | `Error.match is not exhaustive; missing Timeout`<br/>`Error.match is not exhaustive; missing NotFound` | `04-nominal-errors/a-computed-error-match-is-exhaustiveness-checked` (+2) | [Error Prototype](/specification/failures) |
| `VIBE1254` | `Error.match has cases outside the checked union: Timeout` | `04-nominal-errors/error-match-has-a-case-outside-the-union` | — |
| `VIBE1255` | `Error.matchPartial requires an explicit fallback(error) callback as its second argument` | `04-nominal-errors/error-match-partial-needs-a-fallback` | — |
| `VIBE1301` | `Result value is not consumed; return, match, transform, inspect, or propagate it with postfix !`<br/>`this call hands back a collection of Results that is not consumed; read one back out with postfix !, pass the collection to Result.all, or return it`<br/>… and 2 more wording(s) | `07-must-consume/a-collection-laundered-through-an-opaque-return-type-is-still-a-discard` (+31) | [specification/failures](/specification/failures), [Foreign Boundary](/specification/compatibility) |
| `VIBE1302` | `Result 'found' is never consumed`<br/>`Result 'outcome' is never consumed`<br/>… and 3 more wording(s) | `07-must-consume/a-shadowed-result-namespace-does-not-discharge` (+6) | [Matching and Transformation](/specification/failures), [specification/type-system](/specification/type-system) |
| `VIBE1303` | `an inferred-fallible function value cannot cross a callback boundary; add an explicit Result contract or handle its failures before passing it`<br/>`callable contract discards a returned Result at value.return; preserve its return channel and consume it at the invocation` | `07-must-consume/a-fallible-callback-argument-to-a-new-expression-needs-a-contract` (+16) | [Inference and Public Contracts](/specification/failures), [Inference](/specification/failures) |
| `VIBE1401` | `Promise instance .then(), .catch(), and .finally() are unavailable in authored .vibe; consume the Promise with await` | `08-promise-chaining/a-computed-then-on-a-bound-promise-is-rejected` (+5) | [Promise Semantics](/specification/failures), [specification/failures](/specification/failures) |
| `VIBE1402` | `this call hands back started Promises that are not consumed; await a recognized Promise combinator over the collection, or return it`<br/>`started Promise is not consumed with await, return, or an awaited recognized combinator` | `07-must-consume/a-returned-promise-collection-charges-its-caller` (+10) | [specification/requirements](/specification/requirements), [specification/failures](/specification/failures) |
| `VIBE1403` | `Promise 'started' is never consumed with await or return`<br/>`Promise 'name' is never consumed with await or return` | `07-must-consume/a-shadowed-promise-namespace-does-not-discharge` (+3) | [Promise Semantics](/specification/failures), [specification/compatibility](/specification/compatibility) |
| `VIBE1404` | `callable contract discards a returned Promise at value.return; preserve its return channel and consume it at the invocation` | `08-promise-chaining/unowned-async-callback-is-rejected` (+1) | [Scoping](/specification/requirements) |
| `VIBE1502` | `this foreign declaration makes contradictory @throws claims ({TypeError} and {never}); a binding cannot both claim never and declare a channel — keep one @throws tag`<br/>`this foreign declaration makes contradictory @throws claims ({never} and {TypeError}); a binding cannot both claim never and declare a channel — keep one @throws tag`<br/>… and 2 more wording(s) | `09-foreign-calls/a-declared-channel-followed-by-a-never-claim-is-refused-identically` (+5) | [specification/compatibility](/specification/compatibility), [specification/failures](/specification/failures) |
| `VIBE1503` | `panic(...) lowering is supported only as an expression statement or a direct return`<br/>`panic(...) is a call, not a template tag; a tagged 'panic' receives the template parts as an array, so the authored text becomes the panic's cause and the message degrades to the generic 'VibeLang panic'. Write panic('...') instead` | `09-foreign-calls/a-contract-error-does-not-suppress-a-panic-placement-refusal` (+3) | [specification/failures](/specification/failures) |
| `VIBE1504` | `a foreign decorator is invoked when the declaration is evaluated, with no call expression to lower; apply it inside an annotated adapter instead`<br/>`a foreign tagged template invokes its tag and can execute JavaScript, but tagged-template Result lowering is deferred; call the tag as an ordinary function through an annotated adapter (only a checker-resolved @throws {never} tag is accepted)`<br/>… and 2 more wording(s) | `09-foreign-calls/a-foreign-decorator-is-invoked-when-the-declaration-is-evaluated` (+6) | [specification/compatibility](/specification/compatibility), [Foreign Boundary](/specification/compatibility) |
| `VIBE1505` | `top-level Result.expect() panics on the error variant and cannot expose that checked panic channel; move it into a checked Result-returning function and consume that Result`<br/>`top-level panic cannot be represented as a checked Result; move it into a checked Result-returning function and consume that Result` | `01-result-lifting/top-level-expect-is-rejected` (+1) | [Propagation](/specification/failures), [Panic Does Not Widen a Return Type is about a FUNCTION's return type](/specification/failures) |
| `VIBE1506` | `this position invokes Symbol.toPrimitive / valueOf / toString on a foreign value with no call expression to lower; that step can throw, so bind the value through a checker-annotated adapter that returns an owned VibeLang value first`<br/>`this position invokes Symbol.iterator / Symbol.asyncIterator on a foreign value with no call expression to lower; that step can throw, so bind the value through a checker-annotated adapter that returns an owned VibeLang value first`<br/>… and 3 more wording(s) | `09-foreign-calls/a-coercing-operator-on-a-foreign-value-keeps-the-panic-case` (+11) | [specification/compatibility](/specification/compatibility), [Foreign Boundary](/specification/compatibility) |
| `VIBE1507` | `this foreign callee is not a stable reference the compiler can read once, or its result is already an unchecked foreign value; bind the callee to a const and propagate the checked result with postfix !, or continue through an explicitly typed local adapter` | `09-foreign-calls/a-foreign-factory-result-invoked-before-it-is-unwrapped-is-rejected` (+1) | [specification/failures](/specification/failures), [Foreign Boundary](/specification/compatibility) |
| `VIBE1508` | `foreign callable provenance would escape through an unchecked higher-order call; wrap it in a local adapter that owns invocation and exposes an explicit Result/task contract`<br/>`returning an executable foreign value would lose its panic provenance; return a VibeLang-owned adapter with an explicit Result/task contract instead` | `09-foreign-calls/a-foreign-callable-escaping-through-a-local-higher-order-call-is-rejected` (+5) | [specification/failures](/specification/failures), [Foreign Boundary](/specification/compatibility) |
| `VIBE1509` | `a callback handed to an UNTRUSTED foreign call may escape beyond the checked call scope; register it through a trusted @throws {never} binding that owns the invocation, or expose an owned VibeLang wrapper/adapter with an explicit Result or structured-task callback policy` | `09-foreign-calls/a-callback-handed-to-an-untrusted-host-is-still-rejected` (+2) | [Foreign Boundary](/specification/compatibility), [Scoping](/specification/requirements) |
| `VIBE1510` | `statically loaded TypeScript/JavaScript module does not declare a leading @module and @throws {never} initialization trust claim`<br/>`foreign module initialization can panic before a checked call boundary; the module could not be resolved to a declaration carrying a leading @module and @throws {never} trust claim`<br/>… and 4 more wording(s) | `01-result-lifting/a-star-re-export-of-the-compiler-owned-prelude-is-refused` (+19) | [Compiler Lifting](/specification/failures), [Foreign Exceptions now documents its alpha-0 availability](/specification/failures) |
| `VIBE1511` | `a top-level throw cannot be represented as a checked Result; move it into a checked Result-returning function and consume that Result` | `01-result-lifting/top-level-throw-is-rejected` | — |
| `VIBE1601` | `ambient host global 'crypto' is unavailable; access it through a Context capability`<br/>`ambient host global 'self' is unavailable; access it through a Context capability`<br/>… and 26 more wording(s) | `20-host-globals/a-non-random-crypto-member-is-host-authority` (+9) | [Host Globals](/specification/compatibility), [specification/compatibility](/specification/compatibility) |
| `VIBE1602` | `ambient wall-clock access is unavailable; access it through Clock.context()`<br/>`ambient monotonic-clock access is unavailable; access it through Clock.context()` | `09-foreign-calls/a-host-global-inside-a-trusted-registration-is-still-refused` (+11) | [Host Globals](/specification/compatibility), [Determinism-Sensitive Members, ROW FOUR](/specification/compatibility) |
| `VIBE1603` | `ambient randomness is unavailable; access it through Random.context()` | `20-host-globals/a-widening-string-key-escapes-the-other-host-roots` (+2) | [Host Globals](/specification/compatibility) |
| `VIBE1604` | `ambient dynamic code evaluation through 'eval' is unavailable; it runs a string as code in the host's own scope, so the enclosing row cannot describe what it reads, throws, or requires`<br/>`ambient dynamic code evaluation through a callable's 'constructor', which is the Function constructor is unavailable; it runs a string as code in the host's own scope, so the enclosing row cannot describe what it reads, throws, or requires`<br/>… and 1 more wording(s) | `18-typescript-requirement/eval-cannot-be-described-by-a-row-and-is-refused` (+6) | [specification/compatibility](/specification/compatibility), [Host Globals](/specification/compatibility) |
| `VIBE1605` | `ambient host global 'WeakRef' is unavailable; its result is a function of garbage-collection timing or another agent's schedule, which no capability can mediate and no journal entry can describe`<br/>`ambient host global 'FinalizationRegistry' is unavailable; its result is a function of garbage-collection timing or another agent's schedule, which no capability can mediate and no journal entry can describe`<br/>… and 2 more wording(s) | `20-host-globals/determinism-hostile-globals-are-refused` | [Determinism-Sensitive Members](/specification/compatibility) |
| `VIBE1717` | `a conditional declaration requires braced branches throughout its if/else-if/else chain`<br/>`'var' in a conditional declaration hoists out of the conditional construct; use 'const' or 'let'` | `14-conditional-declarations/conditional-declaration-with-a-braceless-branch-is-rejected` (+1) | — |
| `VIBE1801` | `relative VibeLang module './absent-module.mod.vibe' is not present in the supplied project source set` | `15-generic-rows/missing-relative-module-is-rejected` | [Satisfaction](/specification/requirements) |
| `VIBE1802` | `cross-module function 'tag' escapes direct static call analysis; wrap the higher-order use in an explicitly checked local function`<br/>`cross-module function 'count' escapes direct static call analysis; wrap the higher-order use in an explicitly checked local function`<br/>… and 1 more wording(s) | `15-generic-rows/a-tagged-template-callee-is-rejected` (+2) | [Inference](/specification/requirements) |
| `VIBE1803` | `the failure row of generic call 'attempt' is a template over its type parameters and is still unresolved at this call site; supply concrete type arguments or wrap the call in an explicitly checked non-generic function` | `15-generic-rows/unresolved-row-variable-is-rejected` | — |
| `VIBE1804` | `import 'absent' does not resolve to an exported value in './importing-a-name-a-module-does-not-export-is-rejected.mod.vibe'` | `15-generic-rows/importing-a-name-a-module-does-not-export-is-rejected` | [specification/requirements](/specification/requirements) |
| `VIBE1806` | `instantiating 'attempt' here publishes the failure row {NotFound, Timeout}, which a callback argument's declared Conflict cannot produce; correct the type arguments or widen the row` | `15-generic-rows/callback-row-must-be-nominally-covered` | — |
## Requirements and Layers

| Code | Message the compiler emits | Corpus case | Specification |
| --- | --- | --- | --- |
| `VIBE2101` | `Layer.provide is missing {Db}`<br/>`Layer.provide is missing {Registry}`<br/>… and 4 more wording(s) | `05-context-rows/a-coercion-row-is-not-subtracted-by-the-wrong-layer` (+8) | [Satisfaction](/specification/requirements), [Capability Identity](/specification/requirements) |
| `VIBE2102` | `top-level call has unsatisfied requirements {Reader}`<br/>`top-level call has unsatisfied requirements {Clock}`<br/>… and 5 more wording(s) | `05-context-rows/a-callback-capability-escapes-the-top-level-check` (+18) | [Inference](/specification/requirements), [specification/requirements](/specification/requirements) |
| `VIBE2103` | `Layer.provide callback must resolve to a checked local function` | `06-layers/a-provide-callback-that-is-not-a-local-function-is-rejected` | [specification/requirements](/specification/requirements) |
| `VIBE2104` | `Layer expression is opaque; the compiler cannot prove its provided capability closure` | `06-layers/a-capability-argument-that-pins-no-single-class-is-opaque` (+4) | [Satisfaction](/specification/requirements), [Layer Algebra](/specification/requirements) |
| `VIBE2105` | `fallible Layer.provide callbacks need an explicit Result (or Promise&lt;Result&gt;) contract so the provided computation keeps its failure channel` | `06-layers/fallible-provide-callback-needs-a-contract` | — |
| `VIBE2106` | `the receiver of context() must identify exactly one Context subclass; this receiver can evaluate to a different capability at runtime, so no nominal key can be recorded` | `05-context-rows/a-generic-capability-receiver-is-rejected` (+3) | [Context Access](/specification/requirements) |
| `VIBE2107` | `Capability.context is a compiler-recognized capability read, not a value; a detached reference (call/apply/bind, an alias, or handing it to another function) loses the requirement row — invoke it directly as Capability.context()` | `05-context-rows/a-bound-context-reference-is-rejected` (+2) | [Context Access](/specification/requirements) |
## Durable execution

| Code | Message the compiler emits | Corpus case | Specification |
| --- | --- | --- | --- |
| `VIBE4100` | `Property 'value' does not exist on type 'never'.` | `17-durable/an-unannotated-action-signature-has-no-failure-channel` | [Action](/specification/durable-execution) |
| `VIBE4103` | `durable(...) argument is not an inline or statically resolvable function`<br/>`an executable Flow requires one resolved durable(body) declaration` | `17-durable/an-opaque-durable-argument-is-rejected` (+1) | [specification/durable-execution](/specification/durable-execution), [Compiler-Recognized API](/specification/durable-execution) |
| `VIBE4111` | `non-finite numeric literal is not durable` | `17-durable/a-non-finite-numeric-literal-in-a-durable-value-is-rejected` | [Durable Boundary Contract](/specification/durable-execution) |
| `VIBE4113` | `action-run-takes-no-schema-argument.vibe#Step.run requires exactly one input argument in durable source` | `17-durable/action-run-takes-no-schema-argument` | [Action](/specification/durable-execution) |
| `VIBE4199` | `durable effect manifest derivation failed closed: durable Action has no derivable durable contract, so its effect cannot be stated soundly` | `17-durable/a-capability-at-the-durable-boundary-is-rejected` (+3) | [Durable Boundary](/specification/durable-execution), [Durable Boundary Contract requires an unambiguous durable failure identity](/specification/durable-execution) |
## Comptime and asset imports

| Code | Message the compiler emits | Corpus case | Specification |
| --- | --- | --- | --- |
| `VIBE5201` | `non-code imports require 'with { type: "..." }'` | `23-asset-imports/a-computed-import-attribute-value-is-rejected` (+2) | [Built-In Formats](/specification/comptime) |
| `VIBE5202` | `asset imports use 'with { ... }'; legacy import assertions are unsupported` | `23-asset-imports/the-legacy-assert-attribute-spelling-is-rejected` | [Built-In Formats](/specification/comptime) |
| `VIBE5203` | `asset import attribute names must be static identifiers` | `23-asset-imports/a-non-identifier-import-attribute-name-is-rejected` | [Built-In Formats](/specification/comptime) |
| `VIBE5204` | `duplicate asset import attribute 'type'` | `23-asset-imports/a-duplicate-import-attribute-is-rejected` | [Built-In Formats](/specification/comptime) |
| `VIBE5205` | `asset import attribute 'type' must be a string literal` | `23-asset-imports/a-computed-import-attribute-value-is-rejected` (+1) | [Built-In Formats](/specification/comptime) |
| `VIBE5206` | `'export * from' an asset is unsupported; re-export the named bindings or a namespace binding` | `23-asset-imports/a-bare-star-re-export-of-an-asset-is-rejected` | — |
| `VIBE5207` | `asset imports must use a relative path` | `23-asset-imports/a-non-relative-asset-specifier-is-rejected` | — |
| `VIBE5208` | `asset imports require runtime bindings and cannot be type-only or side-effect-only` | `23-asset-imports/a-type-only-asset-import-is-rejected` | [guide/asset-imports](/guide/asset-imports) |
| `VIBE5209` | `ENOENT: no such file or directory, lstat '/private/var/folders/qy/8f0_qzms1vzd_00lv3pp30yw0000gn/T/counter.json'` | `23-asset-imports/an-asset-path-outside-the-project-root-is-rejected` | [guide/asset-imports](/guide/asset-imports) |
| `VIBE5213` | `truncated.json: JSON Parse error: Property name must be a string literal` | `23-asset-imports/a-loader-that-cannot-read-its-asset-fails-the-compile` | — |
| `VIBE5218` | `dynamic asset imports require a literal specifier and a literal 'with { ... }' attribute object` | `23-asset-imports/a-non-literal-dynamic-asset-import-is-rejected` | — |

## Defined but not observed in this SDK corpus run

These codes have report sites in the shared native Go compiler or the thin SDK hosts, but the current SDK corpus run does not observe them. This inventory uses the same comment/test-filtered census as `conformance/COVERAGE.md`, including constructed codes across Go source files. The SDK column includes its shared native engine; these are not independent compiler implementations. A report site is not proof that every emission profile can reach it, nor that a corpus case exercises it.

| Code | SDK + shared Go | Go compiler/bridge |
| --- | --- | --- |
| `VIBE0001` | yes | yes |
| `VIBE0002` | yes | yes |
| `VIBE0003` | yes | yes |
| `VIBE0004` | yes | yes |
| `VIBE1151` | yes | yes |
| `VIBE1204` | yes | yes |
| `VIBE1703` | yes | yes |
| `VIBE1808` | yes | yes |
| `VIBE1809` | yes | yes |
| `VIBE1810` | yes | yes |
| `VIBE1900` | yes | yes |
| `VIBE1901` | yes | yes |
| `VIBE1902` | yes | yes |
| `VIBE1903` | yes | yes |
| `VIBE1904` | yes | yes |
| `VIBE1905` | yes | yes |
| `VIBE1906` | yes | yes |
| `VIBE1908` | yes | yes |
| `VIBE1910` | yes | yes |
| `VIBE1911` | yes | yes |
| `VIBE1912` | yes | yes |
| `VIBE1913` | yes | yes |
| `VIBE4101` | yes | yes |
| `VIBE4102` | yes | yes |
| `VIBE4104` | yes | yes |
| `VIBE4105` | yes | yes |
| `VIBE4108` | yes | yes |
| `VIBE4109` | yes | yes |
| `VIBE4110` | yes | yes |
| `VIBE4112` | yes | yes |
| `VIBE4114` | yes | yes |
| `VIBE4115` | yes | yes |
| `VIBE4116` | yes | yes |
| `VIBE4117` | yes | yes |
| `VIBE4118` | yes | yes |
| `VIBE4119` | yes | yes |
| `VIBE4120` | yes | yes |
| `VIBE4121` | yes | yes |
| `VIBE4122` | yes | yes |
| `VIBE4123` | yes | yes |
| `VIBE4124` | yes | yes |
| `VIBE4200` | yes | yes |
| `VIBE4201` | yes | — |
| `VIBE4202` | yes | yes |
| `VIBE4203` | yes | yes |
| `VIBE5210` | yes | — |
| `VIBE5211` | yes | — |
| `VIBE5212` | yes | — |
| `VIBE5214` | yes | — |
| `VIBE5215` | yes | yes |
| `VIBE5216` | yes | yes |
| `VIBE5217` | yes | yes |
| `VIBE5219` | yes | — |
| `VIBE6001` | yes | yes |
| `VIBE6002` | yes | yes |
| `VIBE6003` | yes | yes |
