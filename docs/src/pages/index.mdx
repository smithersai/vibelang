---
title: Smithers · The Programming language for agents
layout: minimal
description: A TypeScript-derived language with compiler support for reliable tools and agents.
showAskAi: false
showLogo: true
showOutline: false
showSidebar: false
---

import { CopyPromptButton } from '../components/CopyPromptButton'

<div className="smithers-home">
  <div className="smithers-home__art" aria-hidden="true" />

  <section className="smithers-home__hero">
    <div className="smithers-home__example" aria-label="Smithers code example">
      <div className="smithers-home-code__header">
        <span>main.sm</span>
        <span className="smithers-home-code__status"><i /> read only</span>
      </div>
      <pre className="smithers-home-code"><code>
        <span className="line"><i>1</i><b>class</b> <strong>NotFound</strong> <b>extends</b> Error {'{}'}</span>
        <span className="line"><i>2</i></span>
        <span className="line"><i>3</i><b>function</b> <strong>getUser</strong>(id: <em>number</em>): <strong>Result</strong>&lt;<em>string</em>, <strong>NotFound</strong>&gt; {'{'}</span>
        <span className="line"><i>4</i>  <b>if</b> (id !== <mark>1</mark>)</span>
        <span className="line"><i>5</i>    <b>throw new</b> NotFound(<q>"missing"</q>)</span>
        <span className="line"><i>6</i>  <b>return</b> <q>"Ada"</q></span>
        <span className="line"><i>7</i>{'}'}</span>
        <span className="line"><i>8</i></span>
        <span className="line"><i>9</i><b>export function</b> <strong>main</strong>(): <strong>Result</strong>&lt;<em>string</em>, <strong>NotFound</strong>&gt; {'{'}</span>
        <span className="line"><i>10</i>  <b>return</b> getUser(<mark>2</mark>)<strong>!</strong></span>
        <span className="line"><i>11</i>{'}'}</span>
      </code></pre>
      <div className="smithers-home-code__output"><span>output</span><strong>missing</strong></div>
    </div>

    <div className="smithers-home__intro">
      <div className="smithers-home__eyebrow">The Programming language for agents</div>
      <h1><span className="smithers-gradient">Smithers</span></h1>
      <div className="smithers-home__tagline">
        Build <span>reliable agentic systems</span> with the full power of the <span>TypeScript ecosystem</span>.
      </div>
      <div className="smithers-home__actions">
        <CopyPromptButton />
        <button className="smithers-watch-video" type="button" aria-disabled="true">
          <svg viewBox="0 0 20 20" aria-hidden="true"><path d="m7 5 7 5-7 5Z" /></svg>
          Watch Video
        </button>
      </div>
      <div id="smithers-home-install">

  :::code-group{syncKey=""}

  ```bash [npm]
  npm install --save-dev smthrs
  npx smithers init
  ```

  ```bash [pnpm]
  pnpm add --save-dev smthrs
  pnpm exec smithers init
  ```

  ```bash [bun]
  bun add --dev smthrs
  bunx smithers init
  ```

  :::

      </div>
    </div>
  </section>

</div>
