---
title: VibeLang · The programming language for agents
layout: minimal
description: A TypeScript-derived language with compiler support for reliable tools and agents.
showAskAi: false
showLogo: true
showOutline: false
showSidebar: false
---

import { CopyPromptButton } from '../components/CopyPromptButton'

<div className="vibelang-home">
  <div className="vibelang-home__art" aria-hidden="true" />

  <section className="vibelang-home__hero">
    <div className="vibelang-home__example" aria-label="VibeLang code example">
      <div className="vibelang-home-code__header">
        <span>main.vibe</span>
        <span className="vibelang-home-code__status"><i /> read only</span>
      </div>
      <pre className="vibelang-home-code"><code>
        <span className="line"><i>1</i><b>class</b> <strong>NotFound</strong> <b>extends</b> Error {'{}'}</span>
        <span className="line"><i>2</i></span>
        <span className="line"><i>3</i><b>function</b> <strong>getUser</strong>(id: <em>number</em>): <strong>Result</strong>&lt;<em>string</em>, <strong>NotFound</strong>&gt; {'{'}</span>
        <span className="line"><i>4</i>  <b>if</b> (id !== <mark>1</mark>)</span>
        <span className="line"><i>5</i>    <b>throw new</b> NotFound(<q>"missing"</q>)</span>
        <span className="line"><i>6</i>  <b>return</b> <q>"Ada"</q></span>
        <span className="line"><i>7</i>{'}'}</span>
        <span className="line"><i>8</i></span>
        <span className="line"><i>9</i><b>export function</b> <strong>main</strong>(): <strong>Result</strong>&lt;<em>string</em>, <strong>NotFound</strong>&gt; {'{'}</span>
        <span className="line"><i>10</i>  <b>const</b> user = getUser(<mark>2</mark>)<strong>!</strong></span>
        <span className="line"><i>11</i>  <b>return</b> <q>`hello ${'{'}user{'}'}`</q></span>
        <span className="line"><i>12</i>{'}'}</span>
      </code></pre>
      <div className="vibelang-home-code__output"><span>vibe check</span><strong>main · failures: NotFound · requirements: none</strong></div>
    </div>

    <div className="vibelang-home__intro">
      <div className="vibelang-home__eyebrow">The programming language for agents</div>
      <h1><span className="vibelang-gradient">VibeLang</span></h1>
      <div className="vibelang-home__tagline">
        Build <span>reliable agentic systems</span> with the full power of the <span>TypeScript ecosystem</span>.
      </div>
      <div className="vibelang-home__actions">
        <CopyPromptButton />
      </div>
      <div id="vibelang-home-install">

  :::code-group{syncKey=""}

  ```bash [npm]
  npm install --save-dev vibelang
  npx vibe init
  ```

  ```bash [pnpm]
  pnpm add --save-dev vibelang
  pnpm exec vibe init
  ```

  ```bash [bun]
  bun add --dev vibelang
  bunx vibe init
  ```

  :::

      </div>
    </div>
  </section>

</div>
