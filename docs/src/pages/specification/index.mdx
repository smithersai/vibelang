# Specification Status [How to read the VibeLang design specification]

This specification records the target semantic model of VibeLang. It is the
contract the language, toolchain, standard library, and runtime are designed to
satisfy. It does not describe repository progress or the behavior of a
particular implementation.

## Maturity Labels

Every design rule has one of three maturity levels:

| Label | Meaning |
| --- | --- |
| **Locked** | explicitly accepted by the language designer |
| **Direction** | accepted principle whose spelling or mechanics remain open |
| **Open** | discussed but not decided |

Unless a section says otherwise, statements in these specification pages summarize locked decisions. Syntax shown for a directional feature is informative.

## Normative Terms

The words **MUST**, **MUST NOT**, **SHOULD**, **SHOULD NOT**, and **MAY** describe intended conformance requirements. Because the grammar and compiler contract are incomplete, these terms apply only to behavior marked locked.

Examples are non-normative unless the surrounding text states otherwise.

## Conformance Targets

VibeLang defines several distinct subjects of conformance:

- a **source implementation** parses and checks `.vibe` programs
- the **TypeScript backend** preserves TypeScript-compatible source behavior and emits TypeScript/JavaScript artifacts
- a **durable runtime** admits and executes compiler-derived keyed Plans with the Action, approval, journal, recovery and history protocols

## Source of Truth

The published specification pages are the readable target contract. The
[decision ledger](https://github.com/smithersai/vibelang/blob/main/docs/DECISIONS.md)
records accepted design decisions, and the [durable execution
draft](https://github.com/smithersai/vibelang/blob/main/docs/DURABLE_EXECUTION.md)
expands the distributed runtime model.

These documents are one specification set. A conflict between them is a
documentation defect and must be reconciled; implementation behavior does not
resolve it. Until reconciliation, the decision ledger is authoritative for the
specific decision it records.

The September 6 owner decision replaces ordinary-body durable replay with the
[static Plan contract](/specification/durable-execution). The previous
[specification](/specification/history/durable-replay) and
[guide](/guide/history/durable-replay) are historical records. Their Manifest,
source-occurrence journal and mandatory Flow-body retention rules are not
current requirements. Other pages and older drafts still need section-by-section
reconciliation where they describe that withdrawn durable model; general
non-durable Result and capability rules are not withdrawn by this decision.

## Specification–Implementation Gaps

A maturity label says how settled a rule is. It does not say whether the shipped
compiler implements it. Those are independent axes, and this page's opening
sentence — that the specification does not describe the behavior of a particular
implementation — is why they have to be.

Where a gap between this specification and the shipped compiler has actually been
**measured**, it is marked inline so a reader can find it, and marked with its
**direction**, because the two directions call for opposite responses. The
markers are greppable, in the same spirit as the decision ledger's `(PR-n)`
provisionality markers.

| Marker | Direction | Meaning |
| --- | --- | --- |
| **(SA-n)** | specification ahead | this specification promises behavior the shipped compiler refuses today. The marker names the diagnostic codes that refuse it and the conformance cases that certify the refusal. |
| **(IA-n)** | implementation ahead | the shipped compiler already does something this specification does not permit, or forbids. |

What a marker does and does not mean:

- A marker **records** a gap; it does not resolve one. Nothing carrying `(SA-n)` or `(IA-n)` is thereby weakened, and neither side may be quietly edited to match the other.
- An example carrying `(SA-n)` states the contract. It is **not** an example of what compiles today: copied into a `.vibe` file it draws the diagnostics the marker names.
- A conformance case certifying the current refusal is evidence **for** the gap, not evidence against the rule. It MUST stay green and MUST NOT be weakened to fit this specification. It is retired when the implementation moves, and its retirement condition is named at the marker.
- A gap closes when one side is deliberately changed. Until then it is a recorded conflict rather than an unnoticed one.

### Current markers

| Marker | Where | Gap |
| --- | --- | --- |
| **(SA-1), closed 2026-09-05** | [Failure Semantics](/specification/failures) §Refusal Conditions | Both backends now emit expression-position delegated suspension. All six documented forms, conditional operands, repeated loop headers, and propagation after an effect in the same statement compile and run with authored evaluation order. The historical refusal fixtures now assert output; typing and caught-`try` restrictions remain. |
| **(SA-2), current Plan migration** | [Durable Execution](/specification/durable-execution) §Implementation Checkpoint | **Measured native API 48, 2026-09-08:** checked source modules, static fan-out, child composition and pure planned computations produce keyed graphs; `vibe plan` uses this path by default. The public authenticated worker and installed-package path are verified. The production scheduler/Control/journal host, branch/round and other suspension drivers, capability adapters and remaining product-default migration are incomplete. The former ordinary-body checkpoint is historical, not evidence of this contract. |
| **(SA-3), closed 2026-09-05** | [Effects](/specification/effects) §Rows Are Types, §Propagation Is an Abort Request, §Layer Provision Installs a Handler | **Re-measured 2026-09-05: the resumable calling convention is wired.** `poc/src/runtime/index.ts` re-exports the substrate (`__vsRunResult`, `__vsRunResultAsync`, `__vsResultScope`, `__vsGet`, `__vsPerform`, `__vsPropagate`, `__vsProvide*`) and the emitter lowers a fallible function into those delimiters; the 2026-08-28 text is history. *History:* the substrate was implemented but not re-exported and the compiler emitted no call into it; three defects measured in it on 2026-08-28 (failure row keyed on `error.constructor`; a handler able to resume an aborted computation; a forwarding frame dropping `raise`) were fixed before wiring. |
| **(SA-4)** | [Compatibility](/specification/compatibility) §Determinism-Sensitive Members | **Closed 2026-09-01. All five per-member walls are enforced on both backends.** Rows one and two closed on 2026-08-28: `WeakRef`, `FinalizationRegistry`, `SharedArrayBuffer` and `Atomics` were *listed* in the frontend's `UNIVERSAL_GLOBALS` and in the fork's byte-for-byte copy, so the allowlist asserted the opposite of two `MUST NOT`s rather than merely failing to meet them; all four are now `VIBE1605`, a new code because `VIBE1601` offers a Context capability and these rows say no capability can mediate them. Row four closed with the type-directed `dateZoneMemberUse`. **Rows three and five closed 2026-09-01, and they were the interesting ones, because two different things were wrong.** Their verb was settled by decision on 2026-08-28 — they CHARGE (`Promise.race`/`any` publish `Scheduler`, the thirty ICU-backed members publish `Locale`) rather than refuse, by the criterion now stated under Compatibility's table: the ambient spelling is additionally refused only where the capability has a source-language surface the author could write instead, and neither `Scheduler` nor `Locale` has one. The reference implemented both; the fork implemented neither, because its `rowSet` was keyed by `*ast.Symbol` and its `addRow` **silently dropped** a symbol-less row — so an ambient charge could be wired in, compile, and enforce nothing, which is why migration step 14 removed its own wiring rather than ship it. The fork's row now carries an ambient capability by name and `addRow` fails closed on a row it cannot represent. **The second thing was a false claim standing beside the true one**: that the disagreement was invisible to the corpus, because the harness compares stdout, diagnostics and exit code and cannot see a row. A row is not observable — an *unsatisfied* row is, on both backends, as `VIBE2102` at a top-level call naming the capability. Nine shared vectors in `compiler/ambient-charge-vectors.json` now hold the two implementations to the same answer, four of them controls that fail an over-charge. `Error.prototype.stack` remains deliberately undecided rather than folded into row five: ECMA-262 publishes no such property, so the allowlist's own criterion does not reach it. |
| **(SA-5)** | [Compatibility](/specification/compatibility) §Mandatory and §Forbidden | **Closed 2026-08-28.** All six mandatory options are enforced on both backends through one shared table, the eleven forbidden options are rejected as `VIBE6002` with a span, an unclassified option as `VIBE6003`, and a mandatory option that is missing or weakened as `VIBE6001`; `vibe compile -p` / `check -p` now read and validate the project's `tsconfig.json`, which nothing did before. Two corrections to the sizing. The register's claim that none of the eleven forbidden options was rejected was **wrong for the Go fork**, whose bridge already refused unknown option names through a closed allowlist — without a code, file or span, and refusing the five missing mandatory options as "unsupported" rather than requiring them; that allowlist was reused, not replaced. And the `--project` flag was not silently discarded: on a TypeScript input it was forwarded to a spawned `tsc` and on a `.vibe` input refused outright. It was never *read*. The predicted "2 of 515 plus one provenance rule" held for the checker literals and understated the rest: enforcing the options where type errors actually surface moved **150 of 515** until the mandatory set was scoped to `.vibe`, as §Configuration already required — 143 of those were the compiler's own runtime library, which was narrowed. Three of the six options turned out not to be type rules at all: forcing `verbatimModuleSyntax` onto the fork's EMIT program stopped erasing `import { type T }` and RAN the named module's initializer — the fail-open the module-trust rule exists to prevent — so the emit-shaping members are applied to the checking of authored `.vibe` only. `noUncheckedIndexedAccess` widening `results[i]` to `Result<A, E> \| undefined` is now seen through by the `!` provenance walk and asserted by the lowering on both backends, so `arr[i]!` compiles for a `Result` array and is refused for anything else — resolving a contradiction in which the mandated option, the propagation rule, and the specification's own worked example each said something different. |
| **(SA-6), closed 2026-09-05** | [Requirements](/specification/requirements) §Amendment Record, [Capabilities](/reference/capabilities) §Withdrawn | **Re-measured 2026-09-05: the promise-settlement withdrawal is implemented.** `poc/src/runtime/layer.ts` no longer calls `v8.promiseHooks.createHook`; it keeps an `AsyncLocalStorage` frame only to carry the environment, and `test/layer.test.mjs` asserts the opposite of the withdrawn obligations ("an async provider scope is never refused for want of a settlement hook", "a provider body may return a Promise it did not create"). *History:* on 2026-08-28 the file still enforced all three withdrawn obligations and quoted the withdrawn adapter as the remedy; the closure depended on `(SA-3)`. |
| **(SA-7), closed 2026-09-05** | [Failure Semantics](/specification/failures) §Matching and Transformation | **Re-measured 2026-09-05: all twelve required Result members exist.** `andThenAsync`, `tryRecover`, `tryRecoverAsync`, `tapAsync`, `tapErrorAsync`, `tapBothAsync`, `Result.allAsync`, `Result.partition`, `Result.partitionAsync`, and `Result.codec` are declared in `poc/src/runtime/result.ts` and accepted by the checker surface on both backends; `flatten` and `tapBoth` closed on 2026-08-28. `compiler/fork_result_member_surface_test.go` pins the fork's independent copy of the member list against the reference's. *History:* ten of the twelve were absent and refused as `VIBE1301`. |
| **(SA-8), historical closure; new host integration open** | [Durable Execution](/specification/durable-execution) §Compilation Phases and Deployment Authentication | The 2026-09-05 body-envelope closure belongs to the withdrawn model. The current keyed source/provider/runtime artifacts, exact restoration and full invocation-approval target are implemented and tested, including refusal of forged proofs and fabricated provider claims. A packaged Control admission/worker/journal host and its lifecycle controls remain unfinished; graph verification or a signature alone is not admission. |
| **(IA-1), closed 2026-09-06** | [Compatibility](/specification/compatibility) §Dynamic Features | **Decided by the owner: the compiler is right.** `eval`, the `Function` constructor, and `constructor` selection on a callable are refused (`VIBE1604`); the locked sentence permitting them is withdrawn and the corpus cases certifying the refusal are now the contract. *History:* from 2026-08-27 to 2026-09-06 this was the one gap where the implementation was deliberately ahead of the specification. |

`(SA-3)` through `(SA-8)` were measured on 2026-08-28 by a documentation-truth
sweep, each by running the construct rather than by reading source. They were
previously unmarked, which is why the register warns that a maturity label says
nothing about implementation status.

The current end-to-end implementation worklist is
`compiler/IMPLEMENTATION-PLAN.md`; its measurements are checkpoints, not a
ratification of unrelated provisional decisions. The 2026-08-27 durable model
remains in the ledger as history under **Pending ratification**, explicitly
superseded by the September 6 owner decision above it.

## Specification Map

- [Compatibility](/specification/compatibility) defines the TypeScript interop boundary and target model.
- [Type System](/specification/type-system) defines function channels, absence, and boundary precision.
- [Failure Semantics](/specification/failures) defines failures, defects, propagation, and recovery.
- [Effects](/specification/effects) defines effect requests, one-shot delimited continuations, handlers, and effect rows — the shared substrate the failure, requirement, and durable pages are written against.
- [Requirements](/specification/requirements) defines `Context` capabilities, inferred requirement rows, and layer satisfaction.
- [Control Flow](/specification/control-flow) defines the statement model, cleanup, and the one accepted grammar addition.
- [Comptime](/specification/comptime) defines the imported compiler intrinsic, phase evaluation, and tracked compiler input.
- [Durable Execution](/specification/durable-execution) defines the imported durable intrinsic, Actions, static keyed Plans, non-executing inspection, admission, journal/history identity and runtime guarantees.

## Open Design Work

Rules marked **Direction** or **Open** identify design work that remains in the
target contract. They are not implementation status. A directional example
communicates the intended semantics but does not lock its exact syntax, names,
wire format, or operational mechanism.

TypeScript is the sole compilation target. Near-native, LLVM, and general Wasm
targets are outside the language contract. The checked `panic` channel on
foreign calls exists because JavaScript can throw; it is not a portability
classification.
