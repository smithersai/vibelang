"use strict";

// Keep the object identity, enums, factories, and nested namespaces of the
// JavaScript TypeScript API. The direct require form also lets Node expose the
// upstream package's statically detected named exports to ESM consumers.
module.exports = require("typescript-js");

