/** @module @throws {never} */
export const config = { retries: 3 };

// The KEEP-GREEN half. A properly marked module reached at depth two behind a
// properly marked relay: the claim is written in exactly the compact one-line
// form the specification prints, so the chain confers trust at every depth and
// the program compiles and RUNS. Used by
// 09-foreign-calls/a-marked-chain-confers-trust-at-every-depth, which is an
// `expect: "output"` case precisely so this file's initializer really is
// evaluated rather than silently dropped.
