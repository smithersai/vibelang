import { Context } from "smthrs/context"

export abstract class Clock extends Context {
  abstract now(): number
}

export class Stamp {
  get value(): number {
    return Clock.context().now()
  }
  set mark(next: number) {
    Clock.context().now()
  }
}

export function stamp(): Stamp {
  return new Stamp()
}
