export class Missing extends Error {
  constructor(readonly key: string) { super(`no entry for ${key}`) }
}

function lookup(key: string): Result<string, Missing> {
  if (key !== "ada") throw new Missing(key)
  return "Ada Lovelace"
}

/** Published for a caller this module does not contain: an ordinary transfer. */
export function pack(): readonly Result<string, Missing>[] {
  return [lookup("ada")]
}

export function main(): string[] {
  return ["published"]
}
