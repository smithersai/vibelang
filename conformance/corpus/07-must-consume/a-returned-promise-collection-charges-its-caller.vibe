export class Missing extends Error {
  constructor(readonly key: string) { super(`no entry for ${key}`) }
}

async function load(key: string): Promise<string> {
  return key === "ada" ? "Ada Lovelace" : "unknown"
}

function startAll(): readonly Promise<string>[] {
  return [load("ada"), load("grace")]
}

export async function main(): Promise<string[]> {
  const started = startAll()
  return [`${started.length}`]
}
