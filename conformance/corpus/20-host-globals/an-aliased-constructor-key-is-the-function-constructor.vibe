const KEY = "constructor"

export function platform(): string {
  const make = (function () {})[KEY] as (body: string) => () => string
  return String(make("return process.platform")())
}

export const values = [platform()]
