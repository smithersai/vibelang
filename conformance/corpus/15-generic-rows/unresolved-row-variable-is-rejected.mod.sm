export class Timeout extends Error {
  constructor() { super("attempt timed out") }
}

export function attempt<A, E extends Error>(
  limit: number,
  operation: () => Result<A, E>,
): Result<A, E | Timeout> {
  if (limit <= 0) throw new Timeout()
  return operation().unwrap()
}
