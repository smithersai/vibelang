import { attempt, Timeout } from "./unresolved-row-variable-is-rejected.mod.sm"

export function forward<E extends Error>(
  limit: number,
  operation: () => Result<string, E>,
): Result<string, E | Timeout> {
  return attempt(limit, operation).unwrap()
}
