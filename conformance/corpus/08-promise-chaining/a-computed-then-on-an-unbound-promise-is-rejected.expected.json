{
  "title": "chaining .then through a literal element access on a Promise that was never bound is still rejected",
  "expect": "diagnostics",
  "diagnostics": [
    {
      "code": "SMITHERS1401",
      "line": 5,
      "column": 25
    },
    {
      "code": "SMITHERS1402",
      "line": 5,
      "column": 25
    }
  ],
  "notes": "specification/failures.mdx, 'Promise Semantics': 'Authored `.sm` code MUST consume Promise instances only through `await`.' docs/DECISIONS.md (Locked, Concurrency): every started Promise MUST be consumed. specification/failures.mdx: 'Calls to Promise instance `.then()`, `.catch()`, or `.finally()` MUST be compile errors.' This is the unbound spelling of 08-promise-chaining/a-computed-then-on-a-bound-promise-is-rejected, and it was the eleventh member of the Go fail-open class: the fork compiled and ran it. The pair of diagnostics matches the dotted 08-promise-chaining/promise-then-is-rejected exactly — rejecting the chaining spelling also leaves the started Promise unconsumed, so both reports are part of the contract."
}
