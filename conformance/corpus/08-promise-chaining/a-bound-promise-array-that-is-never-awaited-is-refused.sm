async function lookup(key: string): Promise<string> {
  return key === "ada" ? "Ada Lovelace" : "unknown"
}

export async function main(): Promise<string[]> {
  const started = [lookup("ada"), lookup("grace")]
  return [`${started.length}`]
}
