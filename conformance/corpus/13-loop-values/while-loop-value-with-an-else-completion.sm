export function countdown(from: number): string {
  let remaining = from
  const outcome = search: while (remaining > 0) {
    if (remaining === 3) break :search "hit three"
    remaining = remaining - 1
  } else "never hit three"
  return outcome
}

export function main(): string[] {
  return [countdown(5), countdown(2)]
}
