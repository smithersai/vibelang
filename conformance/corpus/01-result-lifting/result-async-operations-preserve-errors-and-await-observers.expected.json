{
  "title": "Result async operations preserve failures, flatten recovery, join inputs, and await observers",
  "expect": "output",
  "stdout": ["3", "7", "unavailable", "unavailable", "4", "0", "5", "6,8", "1,2", "1", "3,4", "1", "ok:4", "missing", "both:5"],
  "notes": "DECISIONS.md Typed failures locks the async, recovery, and collection surface. This exercises authored source through checking, lowering, emission and execution, including recover forwarding an existing error Result without nesting. Collection failures are selected in input order."
}
