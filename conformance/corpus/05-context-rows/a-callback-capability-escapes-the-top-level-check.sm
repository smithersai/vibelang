import { Context } from "smthrs/context"

abstract class Reader extends Context {
  abstract read(key: string): string
}

function readAll(keys: readonly string[]): string[] {
  return keys.map((key) => Reader.context().read(key))
}

export const lines = readAll(["ada"])
