import { Context } from "smthrs/context"

abstract class Clock extends Context {
  abstract now(): number
}

function timestamp(key: string): number {
  return Clock[key]().now()
}

export const stamped = [`${timestamp("context")}`]
