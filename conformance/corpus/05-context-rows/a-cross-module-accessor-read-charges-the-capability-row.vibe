import { Layer } from "vibelang/provider"
import { Clock, Stamp, stamp } from "./a-cross-module-accessor-read-charges-the-capability-row.mod.vibe"

function readStamp(source: Stamp): number {
  return source.value
}

const clock: Clock = { now: () => 7 }

export function main(): string[] {
  return Layer.provide(Layer.succeed(Clock, clock), () => [`${readStamp(stamp())}`])
}
