import { durable, Action } from "vibelang:flows"

class Save extends Action<(input: { callback: () => number }) => Result<{ ok: boolean }, Error>> {}

export const Flow = durable((input: { key: string }) => {
  const saved = Save.run({ callback: () => 4 })
  return { ok: saved === undefined }
})

export function main(): string[] {
  return [Flow.artifactSource]
}
