const names = new Map<number, string>([[1, "Ada"]])

function lookup(id: number): Optional<string> {
  const found = names.get(id)
  if (found === undefined) return undefined
  return found
}

export function main(): string[] {
  const name = lookup(2).unwrap()
  return [name]
}
