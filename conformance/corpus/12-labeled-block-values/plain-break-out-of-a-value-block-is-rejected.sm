export function classify(input: string): string {
  const kind = verdict: {
    // A plain `break verdict` would complete the block without its value.
    if (input.length === 0) break verdict
    break :verdict "short"
  }
  return kind
}

export function main(): string[] {
  return [classify(""), classify("ada")]
}
