{
  "title": "postfix propagation in a loop header is rejected: per-iteration propagation needs control-flow lowering",
  "expect": "diagnostics",
  "diagnostics": [
    {
      "code": "SMITHERS1703",
      "line": 12,
      "column": 31
    }
  ],
  "notes": "Postfix propagation in a loop condition, incrementor, or iteration expression needs per-iteration control-flow lowering. The emitted error path must return the enclosing error variant; hoisting that path in front of a loop would change when it runs, so this placement fails closed."
}
