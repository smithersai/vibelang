{
  "title": "the retired `.?` operator applied directly to a nullable call reports its migration diagnostic",
  "expect": "diagnostics",
  "diagnostics": [
    {
      "code": "SMITHERS1001",
      "line": 7,
      "column": 25
    }
  ],
  "notes": "specification/type-system.mdx, 'Absence': the earlier ?T, payload-capture, orelse, and .? grammar MUST NOT be part of the language; T | undefined with ordinary narrowing and optional chaining is the replacement model. This call form previously exposed a source-map backtracking crash, so both backends must still report SMITHERS1001 at the operator while leaving the nullable return type ordinary."
}
