diff --git a/tsc/internal/ast/flow.go b/tsc/internal/ast/flow.go
index 274a92f7..55390b0b 100644
--- a/tsc/internal/ast/flow.go
+++ b/tsc/internal/ast/flow.go
@@ -42,14 +42,20 @@ type FlowLabel = FlowNode
 
 type FlowSwitchClauseData struct {
 	NodeBase
-	SwitchStatement *Node
-	ClauseStart     int32 // Start index of case/default clause range
-	ClauseEnd       int32 // End index of case/default clause range
+	// SwitchNode is the switch this clause range belongs to. It is a
+	// KindSwitchStatement or — for Smithers's value `switch` — a
+	// KindSwitchExpression, so every consumer must read its clauses through
+	// SwitchClauses and its scrutinee through Expression() rather than through
+	// AsSwitchStatement(), which panics on the expression form. The field was
+	// called SwitchStatement while only one kind could reach it.
+	SwitchNode  *Node
+	ClauseStart int32 // Start index of case/default clause range
+	ClauseEnd   int32 // End index of case/default clause range
 }
 
-func NewFlowSwitchClauseData(switchStatement *Node, clauseStart int, clauseEnd int) *Node {
+func NewFlowSwitchClauseData(switchNode *Node, clauseStart int, clauseEnd int) *Node {
 	node := &FlowSwitchClauseData{}
-	node.SwitchStatement = switchStatement
+	node.SwitchNode = switchNode
 	node.ClauseStart = int32(clauseStart)
 	node.ClauseEnd = int32(clauseEnd)
 	return newNode(KindUnknown, node, NodeFactoryHooks{})
diff --git a/tsc/internal/ast/utilities.go b/tsc/internal/ast/utilities.go
index 640ce53e..c9748e85 100644
--- a/tsc/internal/ast/utilities.go
+++ b/tsc/internal/ast/utilities.go
@@ -480,6 +480,31 @@ func IsIterationStatement(node *Node, lookInLabeledStatements bool) bool {
 	return false
 }
 
+// SwitchCaseBlock returns the CaseBlock of a `switch` in either position.
+//
+// Smithers: a statement `switch` and a value `switch` are two node kinds over
+// one CaseBlock, and flow analysis reaches both through the same
+// FlowSwitchClauseData. Reading the clauses back with AsSwitchStatement()
+// panics on a SwitchExpression, so every such site goes through here instead.
+// The panic on any other kind is deliberate and matches the generic accessors
+// in ast.go: a switch-shaped node kind that is missing an arm here must fail
+// loudly rather than silently answer with no clauses.
+func SwitchCaseBlock(node *Node) *CaseBlockNode {
+	switch node.Kind {
+	case KindSwitchStatement:
+		return node.AsSwitchStatement().CaseBlock
+	case KindSwitchExpression:
+		return node.AsSwitchExpression().CaseBlock
+	}
+	panic("Unhandled case in ast.SwitchCaseBlock: " + node.Kind.String())
+}
+
+// SwitchClauses returns the case and default clauses of a `switch` in either
+// position. See SwitchCaseBlock.
+func SwitchClauses(node *Node) []*Node {
+	return SwitchCaseBlock(node).AsCaseBlock().Clauses.Nodes
+}
+
 // Determines if a node is a property or element access expression
 func IsAccessExpression(node *Node) bool {
 	return node.Kind == KindPropertyAccessExpression || node.Kind == KindElementAccessExpression
@@ -1161,6 +1186,20 @@ func IsFunctionExpressionOrArrowFunction(node *Node) bool {
 
 // Warning: This has the same semantics as the forEach family of functions in that traversal terminates
 // in the event that 'visitor' returns true.
+//
+// The switch below is an allowlist of the statement kinds that can contain a
+// `return`, and in ordinary TypeScript it is complete: a `return` is only legal
+// in a statement position, so nothing reachable only through an expression can
+// be one. Smithers breaks that premise — an expression-position labeled value
+// hangs a whole statement body off an expression, so `const k = verdict: { …
+// return x … }` puts a real `return` under a VariableStatement, which the
+// allowlist does not even enter. Missing it is silent and produces a wrong
+// inferred return type and a wrong .d.ts rather than a diagnostic.
+//
+// In a Smithers file the walk therefore continues through every child, stopping
+// exactly where a new `return` scope begins. That is inert for ordinary
+// TypeScript: the gate is a context flag set only on nodes parsed in a `.sm` or
+// `.sm.ts` file, so no upstream traversal changes shape or cost.
 func ForEachReturnStatement(body *Node, visitor func(stmt *Node) bool) bool {
 	var traverse func(*Node) bool
 	traverse = func(node *Node) bool {
@@ -1172,6 +1211,9 @@ func ForEachReturnStatement(body *Node, visitor func(stmt *Node) bool) bool {
 			KindTryStatement, KindCatchClause:
 			return node.ForEachChild(traverse)
 		}
+		if node.Flags&NodeFlagsSmithers != 0 && !IsFunctionLikeOrClassStaticBlockDeclaration(node) {
+			return node.ForEachChild(traverse)
+		}
 		return false
 	}
 	return traverse(body)
@@ -2054,9 +2096,32 @@ func IsInExpressionContext(node *Node) bool {
 	case KindVariableDeclaration, KindParameter, KindPropertyDeclaration, KindPropertySignature, KindEnumMember, KindPropertyAssignment, KindBindingElement:
 		return parent.Initializer() == node
 	case KindExpressionStatement, KindIfStatement, KindDoStatement, KindWhileStatement, KindReturnStatement, KindWithStatement, KindSwitchStatement,
-		KindCaseClause, KindDefaultClause, KindThrowStatement, KindTypeAssertionExpression, KindAsExpression, KindTemplateSpan, KindComputedPropertyName,
-		KindSatisfiesExpression:
+		KindThrowStatement, KindTypeAssertionExpression, KindAsExpression, KindTemplateSpan, KindComputedPropertyName,
+		KindSatisfiesExpression,
+		// Smithers: `defer expr;` and `errdefer expr;`. Without these two arms an
+		// identifier deferred by name is not an expression node, so getTypeOfNode
+		// answers `any` for it while everything else about the statement is right.
+		KindDeferStatement, KindErrdeferStatement:
 		return parent.Expression() == node
+	// Smithers: a clause's case label AND its value. This arm used to be folded
+	// into the Expression() list above, which was safe upstream only because a
+	// clause's other children are statements and a statement never reaches
+	// IsInExpressionContext. A value clause puts an *expression* there, so the
+	// old arm both answered false for every clause value and — for a `default:`
+	// clause, which has no Expression member to read — panicked outright in
+	// Node.Expression().
+	case KindCaseClause, KindDefaultClause:
+		clause := parent.AsCaseOrDefaultClause()
+		return clause.Expression == node || clause.Value == node
+	// Smithers: `break :label value`. The label is an identifier too, and it is
+	// not an expression, so this compares against Value rather than answering
+	// true for every identifier child.
+	case KindBreakStatement:
+		return parent.AsBreakStatement().Value == node
+	// Smithers: the `else` completion value of `label: loop … else value`. Same
+	// shape: the label itself must keep answering false.
+	case KindLabeledStatement:
+		return parent.AsLabeledStatement().ElseValue == node
 	case KindForStatement:
 		s := parent.AsForStatement()
 		return s.Initializer == node && s.Initializer.Kind != KindVariableDeclarationList || s.Condition == node || s.Incrementor == node
diff --git a/tsc/internal/binder/binder.go b/tsc/internal/binder/binder.go
index b88b16be..243d4a16 100644
--- a/tsc/internal/binder/binder.go
+++ b/tsc/internal/binder/binder.go
@@ -509,9 +509,12 @@ func (b *Binder) createFlowMutation(flags ast.FlowFlags, antecedent *ast.FlowNod
 	return result
 }
 
-func (b *Binder) createFlowSwitchClause(antecedent *ast.FlowNode, switchStatement *ast.Node, clauseStart int, clauseEnd int) *ast.FlowNode {
+// createFlowSwitchClause records the clause range [clauseStart, clauseEnd) of
+// switchNode, which is a KindSwitchStatement or — for Smithers's value `switch`
+// — a KindSwitchExpression.
+func (b *Binder) createFlowSwitchClause(antecedent *ast.FlowNode, switchNode *ast.Node, clauseStart int, clauseEnd int) *ast.FlowNode {
 	setFlowNodeReferenced(antecedent)
-	return b.newFlowNodeEx(ast.FlowFlagsSwitchClause, ast.NewFlowSwitchClauseData(switchStatement, clauseStart, clauseEnd), antecedent)
+	return b.newFlowNodeEx(ast.FlowFlagsSwitchClause, ast.NewFlowSwitchClauseData(switchNode, clauseStart, clauseEnd), antecedent)
 }
 
 func (b *Binder) createFlowCall(antecedent *ast.FlowNode, node *ast.Node) *ast.FlowNode {
@@ -2138,28 +2141,38 @@ func (b *Binder) bindSwitchStatement(node *ast.Node) {
 
 func (b *Binder) bindCaseBlock(node *ast.Node) {
 	// Smithers: a switch *expression* has no fallthrough, no `break`, and no
-	// statements, so every clause is bound from the same pre-switch flow and each
-	// clause value joins into one exit.
+	// statements, so each clause is its own one-clause group bound from the same
+	// pre-switch flow, and each clause value joins into one exit.
 	//
-	// It deliberately creates no FlowSwitchClause node: those carry the switch
-	// node into checker/flow.go, where six sites read it back with
-	// `AsSwitchStatement()` and would panic on a SwitchExpression. The cost is
-	// that the scrutinee is not narrowed inside a clause value, which is
-	// conservative rather than wrong, and is recorded for the semantic lane.
+	// The FlowSwitchClause nodes are what narrow the scrutinee inside a clause
+	// value. They carry the switch node into checker/flow.go, which reads its
+	// clauses through ast.SwitchClauses and its scrutinee through Expression() so
+	// that both switch kinds share one implementation.
 	if node.Parent.Kind == ast.KindSwitchExpression {
+		switchExpression := node.Parent
 		preClauseFlow := b.currentFlow
 		postClauseLabel := b.createBranchLabel()
+		clauses := node.AsCaseBlock().Clauses.Nodes
+		isNarrowingSwitch := switchExpression.Expression().Kind == ast.KindTrueKeyword || isNarrowingExpression(switchExpression.Expression())
 		hasDefault := false
-		for _, clause := range node.AsCaseBlock().Clauses.Nodes {
+		for i, clause := range clauses {
 			hasDefault = hasDefault || clause.Kind == ast.KindDefaultClause
-			b.currentFlow = preClauseFlow
+			preCaseFlow := preClauseFlow
+			if isNarrowingSwitch {
+				preCaseFlow = b.createFlowSwitchClause(preClauseFlow, switchExpression, i, i+1)
+			}
+			b.currentFlow = preCaseFlow
 			b.bind(clause)
 			b.addAntecedent(postClauseLabel, b.currentFlow)
 		}
 		if !hasDefault {
 			// With no default clause the switch can complete without entering any
-			// clause, so control still reaches the code after it.
-			b.addAntecedent(postClauseLabel, preClauseFlow)
+			// clause, so control still reaches the code after it. An empty clause
+			// range is what marks that bypass edge, and it is what makes an
+			// exhaustive value switch's bypass unreachable — the same fact
+			// checkSwitchExpression reads to decide whether the expression's type
+			// carries `undefined`.
+			b.addAntecedent(postClauseLabel, b.createFlowSwitchClause(preClauseFlow, switchExpression, 0, 0))
 		}
 		b.currentFlow = b.finishFlowLabel(postClauseLabel)
 		return
@@ -2203,6 +2216,14 @@ func (b *Binder) bindCaseOrDefaultClause(node *ast.Node) {
 		b.currentFlow = saveCurrentFlow
 	}
 	b.bindEach(clause.Statements.Nodes)
+	// Smithers: a value clause's result. It is always nil for a statement
+	// `switch`. bindChildren dispatches this function instead of bindEachChild,
+	// so without this call nothing binds the value at all and every identifier
+	// inside it carries a nil flow node — not merely unnarrowed by the switch,
+	// but unnarrowed by anything at all, including the enclosing function.
+	if clause.Value != nil {
+		b.bind(clause.Value)
+	}
 }
 
 func (b *Binder) bindExpressionStatement(node *ast.Node) {
@@ -2443,10 +2464,23 @@ func (b *Binder) bindIfExpressionFlow(node *ast.Node) {
 	b.hasFlowEffects = b.hasFlowEffects || saveHasFlowEffects
 }
 
+// bindSwitchExpressionFlow is the Smithers value `switch`. It mirrors
+// bindSwitchStatement's scrutinee handling — `preSwitchCaseFlow` is the flow at
+// which every case *label* is evaluated, before any clause has matched — and
+// leaves the clause joins to bindCaseBlock.
+//
+// Without the preSwitchCaseFlow assignment the label expressions are bound at
+// whatever flow an enclosing switch statement happened to leave behind, or at
+// nil outside one, and every identifier in a case label silently loses its
+// narrowing. There is no break target: a value clause holds an expression, so
+// there is nothing for an unlabeled `break` to leave.
 func (b *Binder) bindSwitchExpressionFlow(node *ast.Node) {
 	expr := node.AsSwitchExpression()
 	b.bind(expr.Expression)
+	savePreSwitchCaseFlow := b.preSwitchCaseFlow
+	b.preSwitchCaseFlow = b.currentFlow
 	b.bind(expr.CaseBlock)
+	b.preSwitchCaseFlow = savePreSwitchCaseFlow
 }
 
 func (b *Binder) bindVariableDeclarationFlow(node *ast.Node) {
diff --git a/tsc/internal/checker/checker.go b/tsc/internal/checker/checker.go
index 31287e3d..58d70898 100644
--- a/tsc/internal/checker/checker.go
+++ b/tsc/internal/checker/checker.go
@@ -11122,7 +11122,13 @@ func (c *Checker) checkSwitchExpression(node *ast.Node, checkMode CheckMode) *Ty
 			types = append(types, c.checkExpressionEx(value, checkMode))
 		}
 	}
-	if firstDefaultClause == nil && !c.isExhaustiveSwitchExpression(expressionType, clauses) {
+	// SMITHERS1716: a switch over a closed literal union whose case labels cover
+	// every member needs no default clause, while an open scrutinee type keeps
+	// the requirement. isExhaustiveSwitch is the same function the statement
+	// `switch` uses; it must be, because the binder's bypass edge for this very
+	// node is judged reachable by that function and by nothing else, so a second
+	// copy here could make the expression's type disagree with its own flow graph.
+	if firstDefaultClause == nil && !c.isExhaustiveSwitch(node) {
 		types = append(types, c.undefinedType)
 	}
 	if len(types) == 0 {
@@ -11131,24 +11137,6 @@ func (c *Checker) checkSwitchExpression(node *ast.Node, checkMode CheckMode) *Ty
 	return c.getUnionTypeEx(types, UnionReductionSubtype, nil, nil)
 }
 
-// isExhaustiveSwitchExpression is the value-switch half of SMITHERS1716: a switch
-// over a closed literal union whose case labels cover every member needs no
-// default clause, while an open scrutinee type keeps the requirement.
-//
-// It re-derives the clause types instead of calling getSwitchClauseTypes, which
-// caches per switch *statement* and reads its node back with AsSwitchStatement().
-func (c *Checker) isExhaustiveSwitchExpression(expressionType *Type, clauses []*ast.Node) bool {
-	t := c.getBaseConstraintOrType(expressionType)
-	if !isLiteralType(t) {
-		return false
-	}
-	switchTypes := core.Map(clauses, c.getTypeOfSwitchClause)
-	if len(switchTypes) == 0 || core.Some(switchTypes, isNeitherUnitTypeNorNever) {
-		return false
-	}
-	return c.eachTypeContainedIn(c.mapType(t, c.getRegularTypeOfLiteralType), switchTypes)
-}
-
 // checkLabeledExpression types the Smithers expression-position labeled block
 // and labeled loop values. Its value comes from the `break :label value` exits
 // that target its own label, plus the loop `else` completion value when there is
diff --git a/tsc/internal/checker/flow.go b/tsc/internal/checker/flow.go
index 03b226c9..d6634e57 100644
--- a/tsc/internal/checker/flow.go
+++ b/tsc/internal/checker/flow.go
@@ -1058,7 +1058,7 @@ func (c *Checker) narrowTypeByOptionalChainContainment(f *FlowState, t *Type, op
 
 func (c *Checker) getTypeAtSwitchClause(f *FlowState, flow *ast.FlowNode) FlowType {
 	data := flow.Node.AsFlowSwitchClauseData()
-	expr := ast.SkipParentheses(data.SwitchStatement.Expression())
+	expr := ast.SkipParentheses(data.SwitchNode.Expression())
 	flowType := c.getTypeAtFlowNode(f, flow.Antecedent)
 	t := flowType.t
 	switch {
@@ -1093,7 +1093,7 @@ func (c *Checker) narrowTypeBySwitchOnDiscriminant(t *Type, data *ast.FlowSwitch
 	// values with unit types, except for the case where
 	// `type` is unknown. In this instance we map object
 	// types to the nonPrimitive type and narrow with that.
-	switchTypes := c.getSwitchClauseTypes(data.SwitchStatement)
+	switchTypes := c.getSwitchClauseTypes(data.SwitchNode)
 	if len(switchTypes) == 0 {
 		return t
 	}
@@ -1155,11 +1155,11 @@ func (c *Checker) narrowTypeBySwitchOnDiscriminant(t *Type, data *ast.FlowSwitch
 }
 
 func (c *Checker) narrowTypeBySwitchOnTypeOf(t *Type, data *ast.FlowSwitchClauseData) *Type {
-	witnesses := c.getSwitchClauseTypeOfWitnesses(data.SwitchStatement)
+	witnesses := c.getSwitchClauseTypeOfWitnesses(data.SwitchNode)
 	if witnesses == nil {
 		return t
 	}
-	clauses := data.SwitchStatement.AsSwitchStatement().CaseBlock.AsCaseBlock().Clauses.Nodes
+	clauses := ast.SwitchClauses(data.SwitchNode)
 	// Equal start and end denotes implicit fallthrough; undefined marks explicit default clause.
 	defaultIndex := core.FindIndex(clauses, func(clause *ast.Node) bool {
 		return clause.Kind == ast.KindDefaultClause
@@ -1185,7 +1185,7 @@ func (c *Checker) narrowTypeBySwitchOnTypeOf(t *Type, data *ast.FlowSwitchClause
 }
 
 func (c *Checker) narrowTypeBySwitchOnTrue(f *FlowState, t *Type, data *ast.FlowSwitchClauseData) *Type {
-	clauses := data.SwitchStatement.AsSwitchStatement().CaseBlock.AsCaseBlock().Clauses.Nodes
+	clauses := ast.SwitchClauses(data.SwitchNode)
 	defaultIndex := core.FindIndex(clauses, func(clause *ast.Node) bool {
 		return clause.Kind == ast.KindDefaultClause
 	})
@@ -1221,7 +1221,7 @@ func (c *Checker) narrowTypeBySwitchOnTrue(f *FlowState, t *Type, data *ast.Flow
 }
 
 func (c *Checker) narrowTypeBySwitchOptionalChainContainment(t *Type, data *ast.FlowSwitchClauseData, clauseCheck func(t *Type) bool) *Type {
-	everyClauseChecks := data.ClauseStart != data.ClauseEnd && core.Every(c.getSwitchClauseTypes(data.SwitchStatement)[data.ClauseStart:data.ClauseEnd], clauseCheck)
+	everyClauseChecks := data.ClauseStart != data.ClauseEnd && core.Every(c.getSwitchClauseTypes(data.SwitchNode)[data.ClauseStart:data.ClauseEnd], clauseCheck)
 	if everyClauseChecks {
 		return c.getTypeWithFacts(t, TypeFactsNEUndefinedOrNull)
 	}
@@ -1232,7 +1232,7 @@ func (c *Checker) narrowTypeBySwitchOnDiscriminantProperty(t *Type, access *ast.
 	if data.ClauseStart < data.ClauseEnd && t.flags&TypeFlagsUnion != 0 {
 		accessedName, _ := c.getAccessedPropertyName(access)
 		if accessedName != "" && c.getKeyPropertyName(t) == accessedName {
-			clauseTypes := c.getSwitchClauseTypes(data.SwitchStatement)[data.ClauseStart:data.ClauseEnd]
+			clauseTypes := c.getSwitchClauseTypes(data.SwitchNode)[data.ClauseStart:data.ClauseEnd]
 			candidate := c.getUnionType(core.Map(clauseTypes, func(s *Type) *Type {
 				result := c.getConstituentTypeForKeyType(t, s)
 				if result != nil {
@@ -1289,7 +1289,7 @@ func (c *Checker) getTypeAtFlowBranchLabel(f *FlowState, flow *ast.FlowNode, ant
 		// If the bypass flow contributes a type we haven't seen yet and the switch statement
 		// isn't exhaustive, process the bypass flow type. Since exhaustiveness checks increase
 		// the risk of circularities, we only want to perform them when they make a difference.
-		if flowType.t.flags&TypeFlagsNever == 0 && !slices.Contains(c.antecedentTypes[antecedentStart:], flowType.t) && !c.isExhaustiveSwitchStatement(bypassFlow.Node.AsFlowSwitchClauseData().SwitchStatement) {
+		if flowType.t.flags&TypeFlagsNever == 0 && !slices.Contains(c.antecedentTypes[antecedentStart:], flowType.t) && !c.isExhaustiveSwitch(bypassFlow.Node.AsFlowSwitchClauseData().SwitchNode) {
 			if flowType.t == f.declaredType && f.declaredType == f.initialType {
 				c.antecedentTypes = c.antecedentTypes[:antecedentStart]
 				return FlowType{t: flowType.t}
@@ -1930,12 +1930,21 @@ func isCoercibleUnderDoubleEquals(source *Type, target *Type) bool {
 		target.flags&(TypeFlagsNumber|TypeFlagsString|TypeFlagsBoolean) != 0
 }
 
-func (c *Checker) isExhaustiveSwitchStatement(node *ast.Node) bool {
+// isExhaustiveSwitch answers whether every value of the scrutinee is covered by
+// some case clause. node is a KindSwitchStatement or — for Smithers's value
+// `switch` — a KindSwitchExpression; it reads the scrutinee through
+// Expression() and the clauses through ast.SwitchClauses, so both kinds share
+// one answer. There is deliberately no second implementation for the expression
+// form: checkSwitchExpression decides whether the expression's type carries
+// `undefined` from this function, and the binder's empty-range bypass edge is
+// judged unreachable by this function, so a disagreement between two copies
+// would be a type that says one thing and a flow graph that says another.
+func (c *Checker) isExhaustiveSwitch(node *ast.Node) bool {
 	links := c.switchStatementLinks.Get(node)
 	if links.exhaustiveState == ExhaustiveStateUnknown {
 		// Indicate resolution is in process
 		links.exhaustiveState = ExhaustiveStateComputing
-		isExhaustive := c.computeExhaustiveSwitchStatement(node)
+		isExhaustive := c.computeExhaustiveSwitch(node)
 		if links.exhaustiveState == ExhaustiveStateComputing {
 			links.exhaustiveState = core.IfElse(isExhaustive, ExhaustiveStateTrue, ExhaustiveStateFalse)
 		}
@@ -1946,7 +1955,7 @@ func (c *Checker) isExhaustiveSwitchStatement(node *ast.Node) bool {
 	return links.exhaustiveState == ExhaustiveStateTrue
 }
 
-func (c *Checker) computeExhaustiveSwitchStatement(node *ast.Node) bool {
+func (c *Checker) computeExhaustiveSwitch(node *ast.Node) bool {
 	if ast.IsTypeOfExpression(node.Expression()) {
 		witnesses := c.getSwitchClauseTypeOfWitnesses(node)
 		if witnesses == nil {
@@ -1989,7 +1998,7 @@ func (c *Checker) eachTypeContainedIn(source *Type, types []*Type) bool {
 func (c *Checker) getSwitchClauseTypeOfWitnesses(node *ast.Node) []string {
 	links := c.switchStatementLinks.Get(node)
 	if !links.witnessesComputed {
-		clauses := node.AsSwitchStatement().CaseBlock.AsCaseBlock().Clauses.Nodes
+		clauses := ast.SwitchClauses(node)
 		witnesses := make([]string, len(clauses))
 		for i, clause := range clauses {
 			if clause.Kind == ast.KindCaseClause {
@@ -2026,7 +2035,7 @@ func (c *Checker) getNotEqualFactsFromTypeofSwitch(start int, end int, witnesses
 func (c *Checker) getSwitchClauseTypes(node *ast.Node) []*Type {
 	links := c.switchStatementLinks.Get(node)
 	if !links.switchTypesComputed {
-		clauses := node.AsSwitchStatement().CaseBlock.AsCaseBlock().Clauses.Nodes
+		clauses := ast.SwitchClauses(node)
 		types := make([]*Type, len(clauses))
 		for i, clause := range clauses {
 			types[i] = c.getTypeOfSwitchClause(clause)
@@ -2569,7 +2578,7 @@ func (c *Checker) isReachableFlowNodeWorker(f *FlowState, flow *ast.FlowNode, no
 			// The control flow path representing an unmatched value in a switch statement with
 			// no default clause is unreachable if the switch statement is exhaustive.
 			data := flow.Node.AsFlowSwitchClauseData()
-			if data.ClauseStart == data.ClauseEnd && c.isExhaustiveSwitchStatement(data.SwitchStatement) {
+			if data.ClauseStart == data.ClauseEnd && c.isExhaustiveSwitch(data.SwitchNode) {
 				return false
 			}
 			flow = flow.Antecedent
