diff --git a/tools/scripts/tsc/ast.json b/tools/scripts/tsc/ast.json
index ab8fb5c4..ba90a481 100644
--- a/tools/scripts/tsc/ast.json
+++ b/tools/scripts/tsc/ast.json
@@ -1655,6 +1655,11 @@
                         "name": "Statement",
                         "type": "Statement",
                         "visit": "embeddedStatement"
+                    },
+                    {
+                        "name": "ElseValue",
+                        "type": "Expression",
+                        "optional": true
                     }
                 ]
             },
diff --git a/tsc/internal/binder/binder.go b/tsc/internal/binder/binder.go
index 96db76a7..e49960c1 100644
--- a/tsc/internal/binder/binder.go
+++ b/tsc/internal/binder/binder.go
@@ -85,12 +85,20 @@ type ActiveLabel struct {
 	next           *ActiveLabel
 	breakTarget    *ast.FlowLabel
 	continueTarget *ast.FlowLabel
-	name           string
-	referenced     bool
+	// Smithers: a labeled loop with an `else` completion value has two exits.
+	// breakTarget is the ordinary one, placed *before* the else value so that
+	// normal completion and a valueless `break label` flow into it;
+	// valueBreakTarget is the exit of the whole construct, which a
+	// `break :label value` reaches directly, skipping the else value. Without an
+	// else value the two are the same label.
+	valueBreakTarget *ast.FlowLabel
+	name             string
+	referenced       bool
 }
 
-func (label *ActiveLabel) BreakTarget() *ast.FlowNode    { return label.breakTarget }
-func (label *ActiveLabel) ContinueTarget() *ast.FlowNode { return label.continueTarget }
+func (label *ActiveLabel) BreakTarget() *ast.FlowNode      { return label.breakTarget }
+func (label *ActiveLabel) ContinueTarget() *ast.FlowNode   { return label.continueTarget }
+func (label *ActiveLabel) ValueBreakTarget() *ast.FlowNode { return label.valueBreakTarget }
 
 func BindSourceFile(file *ast.SourceFile) {
 	// This is constructed this way to make the compiler "out-line" the function,
@@ -1980,6 +1988,10 @@ func (b *Binder) bindBreakStatement(node *ast.Node) {
 	// unreachable flow node to a reachable expression.
 	if value := node.AsBreakStatement().Value; value != nil {
 		b.bind(value)
+		// A value break completes the whole labeled construct, so it targets the
+		// label's value exit and skips a loop `else` completion value.
+		b.bindBreakOrContinueStatement(node.Label(), b.currentBreakTarget, (*ActiveLabel).ValueBreakTarget)
+		return
 	}
 	b.bindBreakOrContinueStatement(node.Label(), b.currentBreakTarget, (*ActiveLabel).BreakTarget)
 }
@@ -2181,12 +2193,24 @@ func (b *Binder) maybeBindExpressionFlowIfCall(node *ast.Node) {
 func (b *Binder) bindLabeledStatement(node *ast.Node) {
 	stmt := node.AsLabeledStatement()
 	postStatementLabel := b.createBranchLabel()
+	// Smithers: `label: loop else completionValue`. The else value is a real new
+	// edge in the flow graph, not just extra syntax: it needs its own join point,
+	// placed between the loop's exit and the exit of the construct, so that
+	// everything which merely *ends* the loop flows through it while a value
+	// break jumps over it.
+	breakTarget := postStatementLabel
+	var preElseLabel *ast.FlowLabel
+	if stmt.ElseValue != nil {
+		preElseLabel = b.createBranchLabel()
+		breakTarget = preElseLabel
+	}
 	b.activeLabelList = &ActiveLabel{
-		next:           b.activeLabelList,
-		name:           stmt.Label.Text(),
-		breakTarget:    postStatementLabel,
-		continueTarget: nil,
-		referenced:     false,
+		next:             b.activeLabelList,
+		name:             stmt.Label.Text(),
+		breakTarget:      breakTarget,
+		valueBreakTarget: postStatementLabel,
+		continueTarget:   nil,
+		referenced:       false,
 	}
 	b.bind(stmt.Label)
 	b.bind(stmt.Statement)
@@ -2195,6 +2219,14 @@ func (b *Binder) bindLabeledStatement(node *ast.Node) {
 		stmt.Label.Flags |= ast.NodeFlagsUnreachable
 	}
 	b.activeLabelList = b.activeLabelList.next
+	if preElseLabel != nil {
+		// Normal loop completion and every valueless break to this label arrive
+		// here; the else value is evaluated on exactly those paths and its own
+		// completion continues to the end of the construct.
+		b.addAntecedent(preElseLabel, b.currentFlow)
+		b.currentFlow = b.finishFlowLabel(preElseLabel)
+		b.bind(stmt.ElseValue)
+	}
 	b.addAntecedent(postStatementLabel, b.currentFlow)
 	b.currentFlow = b.finishFlowLabel(postStatementLabel)
 }
diff --git a/tsc/internal/checker/checker.go b/tsc/internal/checker/checker.go
index 865fa666..cc286b85 100644
--- a/tsc/internal/checker/checker.go
+++ b/tsc/internal/checker/checker.go
@@ -4253,6 +4253,12 @@ func (c *Checker) checkLabeledStatement(node *ast.Node) {
 		c.errorOrSuggestion(c.compilerOptions.AllowUnusedLabels == core.TSFalse, labelNode, diagnostics.Unused_label)
 	}
 	c.checkSourceElement(labeledStatement.Statement)
+	// Smithers: the loop `else` completion value must be checked here.
+	// checkSourceElementWorker has no default arm, so an unchecked else value is
+	// silently NOT type-checked rather than reported.
+	if labeledStatement.ElseValue != nil {
+		c.checkExpression(labeledStatement.ElseValue)
+	}
 }
 
 func (c *Checker) checkThrowStatement(node *ast.Node) {
diff --git a/tsc/internal/parser/parser.go b/tsc/internal/parser/parser.go
index 4550d1d1..dee2d78d 100644
--- a/tsc/internal/parser/parser.go
+++ b/tsc/internal/parser/parser.go
@@ -1611,7 +1611,24 @@ func (p *Parser) parseExpressionOrLabeledStatement() *ast.Statement {
 	expression := p.parseExpression()
 
 	if expression.Kind == ast.KindIdentifier && p.parseOptional(ast.KindColonToken) {
-		result := p.finishNode(p.factory.NewLabeledStatement(expression, p.parseStatement()), pos)
+		statement := p.parseStatement()
+		// Smithers: `Identifier : IterationStatement else Expression ;` — the loop
+		// `else` completion value. It is the value of the construct when the loop
+		// ends without a value break: normal completion, `continue` off the end, an
+		// unlabeled `break`, and a plain `break label` all reach it, while
+		// `break :label value` carries its own value past it.
+		//
+		// The `else` is only claimed for an iteration statement, so
+		// `label: { ... } else x` stays a stray `else` and fails closed. Nothing
+		// else can follow a loop body, so the form is conflict-free.
+		var elseValue *ast.Expression
+		if p.contextFlags&ast.NodeFlagsSmithers != 0 && p.token == ast.KindElseKeyword &&
+			ast.IsIterationStatement(statement, false /*lookInLabeledStatements*/) {
+			p.nextToken()
+			elseValue = p.parseExpressionAllowIn()
+			p.parseSemicolon()
+		}
+		result := p.finishNode(p.factory.NewLabeledStatement(expression, statement, elseValue), pos)
 		p.withJSDoc(result, jsdoc)
 		return result
 	}
diff --git a/tsc/internal/printer/factory.go b/tsc/internal/printer/factory.go
index 9a25da3a..bc656faf 100644
--- a/tsc/internal/printer/factory.go
+++ b/tsc/internal/printer/factory.go
@@ -310,6 +310,7 @@ func (f *NodeFactory) RestoreEnclosingLabel(node *ast.Node, outermostLabeledStat
 		outermostLabeledStatement,
 		outermostLabeledStatement.Label,
 		innerLabel,
+		outermostLabeledStatement.ElseValue,
 	)
 }
 
diff --git a/tsc/internal/printer/printer.go b/tsc/internal/printer/printer.go
index bc60255d..e08daac9 100644
--- a/tsc/internal/printer/printer.go
+++ b/tsc/internal/printer/printer.go
@@ -3666,6 +3666,15 @@ func (p *Printer) emitLabeledStatement(node *ast.LabeledStatement) {
 	p.writeSpace()
 	p.emitStatement(node.Statement)
 
+	// Smithers: `label: loop else completionValue;`
+	if node.ElseValue != nil {
+		p.writeSpace()
+		p.emitToken(ast.KindElseKeyword, node.Statement.End(), WriteKindKeyword, node.AsNode())
+		p.writeSpace()
+		p.emitExpressionNoASI(node.ElseValue, ast.OperatorPrecedenceLowest)
+		p.writeTrailingSemicolon()
+	}
+
 	p.exitNode(node.AsNode(), state)
 }
 
diff --git a/tsc/internal/transformers/moduletransforms/commonjsmodule.go b/tsc/internal/transformers/moduletransforms/commonjsmodule.go
index 38061bae..2deb7ef9 100644
--- a/tsc/internal/transformers/moduletransforms/commonjsmodule.go
+++ b/tsc/internal/transformers/moduletransforms/commonjsmodule.go
@@ -1254,7 +1254,7 @@ func (tx *CommonJSModuleTransformer) visitTopLevelNestedLabeledStatement(node *a
 	if statement == nil {
 		statement = tx.Factory().NewEmptyStatement()
 	}
-	return tx.Factory().UpdateLabeledStatement(node, node.Label, statement)
+	return tx.Factory().UpdateLabeledStatement(node, node.Label, statement, tx.Visitor().VisitNode(node.ElseValue))
 }
 
 // Visits a top-level nested `with` statement as it may contain `var` declarations that are hoisted and may still be
