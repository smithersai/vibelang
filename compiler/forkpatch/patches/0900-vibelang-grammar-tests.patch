diff --git a/tsc/internal/checker/vibelang_grammar_test.go b/tsc/internal/checker/vibelang_grammar_test.go
new file mode 100644
index 00000000..f44d926a
--- /dev/null
+++ b/tsc/internal/checker/vibelang_grammar_test.go
@@ -0,0 +1,107 @@
+package checker_test
+
+import (
+	"slices"
+	"testing"
+
+	"github.com/microsoft/TypeScript/tsc/internal/ast"
+	"github.com/microsoft/TypeScript/tsc/internal/bundled"
+	"github.com/microsoft/TypeScript/tsc/internal/compiler"
+	"github.com/microsoft/TypeScript/tsc/internal/core"
+	"github.com/microsoft/TypeScript/tsc/internal/tsoptions"
+	"github.com/microsoft/TypeScript/tsc/internal/vfs/vfstest"
+	"gotest.tools/v3/assert"
+)
+
+const vibelangConditionalTestFile = "/main.vibe.ts"
+const vibelangConditionalTestConfig = `{ "compilerOptions": { "strict": true, "noEmit": true }, "files": ["main.vibe.ts"] }`
+
+func vibelangConditionalDiagnosticCodes(t *testing.T, content string) []int32 {
+	t.Helper()
+	return vibelangConditionalDiagnosticCodesWithOptions(t, content, vibelangConditionalTestConfig)
+}
+
+func vibelangConditionalDiagnosticCodesWithOptions(t *testing.T, content string, config string) []int32 {
+	t.Helper()
+	fs := vfstest.FromMap(map[string]string{
+		vibelangConditionalTestFile: content,
+		"/tsconfig.json": config,
+	}, false /*useCaseSensitiveFileNames*/)
+	fs = bundled.WrapFS(fs)
+	host := compiler.NewCompilerHost("/", fs, bundled.LibPath(), nil, nil, nil)
+	parsed, errors := tsoptions.GetParsedCommandLineOfConfigFile("/tsconfig.json", &core.CompilerOptions{}, nil, host, nil)
+	assert.Equal(t, len(errors), 0, "unexpected command line errors")
+	program := compiler.NewProgram(compiler.ProgramOptions{Config: parsed, Host: host})
+	file := program.GetSourceFile(vibelangConditionalTestFile)
+	assert.Assert(t, file != nil, "source file missing from program")
+	syntactic := program.GetSyntacticDiagnostics(t.Context(), file)
+	assert.Equal(t, len(syntactic), 0, "unexpected syntactic diagnostics: %v", conditionalDiagnosticCodes(syntactic))
+	return conditionalDiagnosticCodes(program.GetSemanticDiagnostics(t.Context(), file))
+}
+
+func conditionalDiagnosticCodes(diagnostics []*ast.Diagnostic) []int32 {
+	codes := make([]int32, 0, len(diagnostics))
+	for _, diagnostic := range diagnostics {
+		codes = append(codes, diagnostic.Code())
+	}
+	slices.Sort(codes)
+	return codes
+}
+
+func TestVibeLangConditionalDeclarationScope(t *testing.T) {
+	t.Parallel()
+	const prelude = `declare function get(): { name: string } | null;
+declare function takes(value: { name: string }): void;
+declare function use(value: unknown): void;
+`
+	data := []struct {
+		title    string
+		body     string
+		expected []int32
+	}{
+		{"narrowed in the then branch", "if (const u = get(); u !== null) { takes(u); }", []int32{}},
+		{"visible in the else branch", "if (const u = get(); u !== null) { takes(u); } else { use(u); }", []int32{}},
+		{"narrowed in the else branch", "if (const u = get(); u !== null) { use(u); } else { const n: null = u; use(n); }", []int32{}},
+		{"the else branch lacks the then narrowing", "if (const u = get(); u !== null) { use(u); } else { takes(u); }", []int32{2345}},
+		{"out of scope after the construct", "if (const u = get(); u !== null) { takes(u); } use(u);", []int32{2304}},
+		{"else-if scopes compose", "if (const a = get(); a !== null) { takes(a); } else if (const b = get(); b !== null) { takes(b); use(a); }", []int32{}},
+		{"an outer name is restored afterward", "const u = 1; if (const u = get(); u !== null) { takes(u); } use(u.toFixed(2));", []int32{}},
+	}
+	for _, rec := range data {
+		t.Run(rec.title, func(t *testing.T) {
+			t.Parallel()
+			assert.DeepEqual(t, vibelangConditionalDiagnosticCodes(t, prelude+rec.body), rec.expected)
+		})
+	}
+}
+
+func TestVibeLangConditionalDeclarationInitializerIsChecked(t *testing.T) {
+	t.Parallel()
+	const prelude = `declare function use(value: unknown): void;`
+	data := []struct {
+		title    string
+		body     string
+		expected []int32
+	}{
+		{"an unreferenced initializer is checked", "if (const ignored = totallyUndefinedIdentifier(); true) { use(1); }", []int32{2304}},
+		{"an initializer is checked against its annotation", "if (const n: string = 42; n) { use(n); }", []int32{2322}},
+		{"an uninitialized const is rejected", "if (const n; true) { use(n); }", []int32{1155, 7005}},
+	}
+	for _, rec := range data {
+		t.Run(rec.title, func(t *testing.T) {
+			t.Parallel()
+			assert.DeepEqual(t, vibelangConditionalDiagnosticCodes(t, prelude+rec.body), rec.expected)
+		})
+	}
+}
+
+func TestVibeLangConditionalDeclarationLocals(t *testing.T) {
+	t.Parallel()
+	const config = `{ "compilerOptions": { "strict": true, "noEmit": true, "noUnusedLocals": true }, "files": ["main.vibe.ts"] }`
+	codes := vibelangConditionalDiagnosticCodesWithOptions(t,
+		"declare function get(): number; if (const unusedBinding = get(); get()) { get(); }", config)
+	assert.DeepEqual(t, codes, []int32{6133})
+	codes = vibelangConditionalDiagnosticCodesWithOptions(t,
+		"declare function get(): number; if (const usedBinding = get(); usedBinding > 1) { get(); }", config)
+	assert.DeepEqual(t, codes, []int32{})
+}
diff --git a/tsc/internal/printer/vibelang_grammar_test.go b/tsc/internal/printer/vibelang_grammar_test.go
new file mode 100644
index 00000000..c521278e
--- /dev/null
+++ b/tsc/internal/printer/vibelang_grammar_test.go
@@ -0,0 +1,92 @@
+package printer_test
+
+import (
+	"strings"
+	"testing"
+
+	"github.com/microsoft/TypeScript/tsc/internal/ast"
+	"github.com/microsoft/TypeScript/tsc/internal/core"
+	"github.com/microsoft/TypeScript/tsc/internal/parser"
+	"github.com/microsoft/TypeScript/tsc/internal/printer"
+	"github.com/microsoft/TypeScript/tsc/internal/tspath"
+	"gotest.tools/v3/assert"
+)
+
+func parseVibeLangConditional(fileName string, text string) *ast.SourceFile {
+	return parser.ParseSourceFile(ast.SourceFileParseOptions{
+		FileName: fileName,
+		Path:     tspath.Path(fileName),
+	}, text, core.ScriptKindTS)
+}
+
+func printVibeLangConditional(file *ast.SourceFile) string {
+	p := printer.NewPrinter(
+		printer.PrinterOptions{NewLine: core.NewLineKindLF},
+		printer.PrintHandlers{},
+		nil,
+	)
+	return strings.TrimSuffix(p.EmitSourceFile(file), "\n")
+}
+
+func TestVibeLangConditionalDeclarationRoundTrip(t *testing.T) {
+	t.Parallel()
+	data := []struct {
+		title string
+		input string
+	}{
+		{"conditional const", "if (const u = lookup(k); u !== null) {\n    use(u);\n}"},
+		{"conditional let", "if (let u = lookup(k); u) {\n    use(u);\n}"},
+		{"conditional destructuring", "if (const { a, b } = pair(); a) {\n    use(b);\n}"},
+		{"conditional with else", "if (const u = lookup(k); u) {\n    use(u);\n}\nelse {\n    other(u);\n}"},
+		{"conditional else-if chain", "if (const a = f(); a) {\n    use(a);\n}\nelse if (const b = g(); b) {\n    use(b);\n}"},
+		{"plain if unchanged", "if (c) {\n    use(1);\n}"},
+		{"let remains an identifier", "if (let > 3) {\n    use(1);\n}"},
+	}
+	for _, rec := range data {
+		t.Run(rec.title, func(t *testing.T) {
+			t.Parallel()
+			for _, fileName := range []string{"/main.vibe", "/main.vibe.ts"} {
+				file := parseVibeLangConditional(fileName, rec.input)
+				assert.Equal(t, len(file.Diagnostics()), 0, "unexpected parse diagnostics in "+fileName)
+				actual := printVibeLangConditional(file)
+				file = parseVibeLangConditional(fileName, actual)
+				assert.Equal(t, len(file.Diagnostics()), 0, "unexpected reparse diagnostics")
+				assert.Equal(t, printVibeLangConditional(file), actual, "printer is not idempotent")
+			}
+		})
+	}
+}
+
+func TestVibeLangConditionalDeclarationNodeShape(t *testing.T) {
+	t.Parallel()
+	file := parseVibeLangConditional("/main.vibe", "if (const u = f(); u) { g(u); } else { h(u); }")
+	assert.Equal(t, len(file.Diagnostics()), 0)
+	statement := file.Statements.Nodes[0]
+	assert.Equal(t, statement.Kind, ast.KindIfStatement)
+	assert.Assert(t, statement.AsIfStatement().Initializer != nil)
+	assert.Assert(t, ast.IsVariableDeclarationList(statement.AsIfStatement().Initializer))
+	var children []ast.Kind
+	statement.ForEachChild(func(node *ast.Node) bool {
+		children = append(children, node.Kind)
+		return false
+	})
+	assert.DeepEqual(t, children, []ast.Kind{
+		ast.KindVariableDeclarationList, ast.KindIdentifier, ast.KindBlock, ast.KindBlock,
+	})
+	assert.Assert(t, ast.IsBlockScope(statement, statement.Parent),
+		"an if with a conditional declaration must be a block scope")
+	plain := parseVibeLangConditional("/main.vibe", "if (u) { g(u); }").Statements.Nodes[0]
+	assert.Assert(t, plain.AsIfStatement().Initializer == nil)
+	assert.Assert(t, !ast.IsBlockScope(plain, plain.Parent), "a plain if must not become a block scope")
+}
+
+func TestVibeLangConditionalDeclarationDialectGate(t *testing.T) {
+	t.Parallel()
+	tsFile := parseVibeLangConditional("/main.ts", "if (const u = f(); u) { g(u); }")
+	assert.Assert(t, len(tsFile.Diagnostics()) > 0, "a conditional declaration must not be accepted in .ts")
+	assert.Assert(t, tsFile.Statements.Nodes[0].AsIfStatement().Initializer == nil,
+		"a .ts if must never carry a conditional declaration")
+	file := parseVibeLangConditional("/main.vibe", "if (let > 3) { g(); }")
+	assert.Equal(t, len(file.Diagnostics()), 0, "let must remain an identifier in a condition")
+	assert.Assert(t, file.Statements.Nodes[0].AsIfStatement().Initializer == nil)
+}
